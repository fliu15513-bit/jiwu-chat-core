# ============================================================
# JiwuChat Docker 配置示例（复制为 .env 后按需修改）
# 构建：docker compose up -d --build  会读取本文件
# 说明：详见 CONFIG.md
# ============================================================

# ---------- Docker 镜像与端口 ----------
# 默认使用 Docker Hub 官方镜像；国内网络需要时可改为可信镜像加速地址
DOCKER_REGISTRY=docker.io

# ---------- 前端（运行时注入，无需重新构建镜像）----------
# 后端 API 与 WebSocket（浏览器实际访问的地址，若通过 Nginx 反代请改为实际域名）
# 修改后重启容器即可生效：docker compose up -d
VITE_API_BASE_URL=http://localhost:9090/
VITE_API_WS_BASE_URL=ws://localhost:9091/
# 七牛 OSS 等（可选）
# VITE_BASE_OSS_PATH=https://oss.example.com/

# ---------- 中间件默认账号（与官方镜像一致，可按需覆盖）----------
# MySQL 8 默认 root 密码（compose 默认：password）
# MYSQL_ROOT_PASSWORD=password
# RabbitMQ 默认用户（官方默认 guest/guest）
# RABBITMQ_DEFAULT_USER=guest
# RABBITMQ_DEFAULT_PASS=guest

# ---------- 后端：数据库（对应 application-prod.yml）----------
# 由 docker-compose 自动注入容器内地址，一般无需改；自建 MySQL 时可在下方覆盖
# SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/jiwu-chat-db?...
# SPRING_DATASOURCE_USERNAME=root
# SPRING_DATASOURCE_PASSWORD=password

# ---------- 后端：邮件（对应 application.yml mail.*）----------
# MAIL_HOST=
# MAIL_PORT=25
# MAIL_USERNAME=
# MAIL_PASSWORD=

# ---------- 后端：七牛云（对应 application.yml qi-niu-cloud）----------
# QINIU_ACCESS_KEY=
# QINIU_SECRET_KEY=
# QINIU_BUCKET=
# QINIU_HOST=
# QINIU_SIGN_KEY=
# QINIU_SIGN_KEY2=

# ---------- 后端：短信 UniSMS（对应 application.yml uni-sms）----------
# UNISMS_ACCESS_KEY=
# UNISMS_SECRET_KEY=
# UNISMS_TEMPLATE_ID=
# UNISMS_SIGNATURE=
# UNI_SMS_WEBHOOK_SIGNATURE_SECRET=

# ---------- 后端：腾讯翻译（对应 application.yml tencent）----------
# TENCENT_SECRET_ID=
# TENCENT_SECRET_KEY=

# ---------- 后端：AI 翻译 / MCP（对应 application.yml）----------
# RES_TRANSLATION_API_URL=https://api.siliconflow.cn/v1
# RES_TRANSLATION_API_KEY=
# RES_TRANSLATION_MODEL=Qwen/Qwen2.5-32B-Instruct
# RES_TRANSLATION_MAX_TOKENS=4096
# MCP_ALLOWED_KEY=
